# Add this file to your catalog-api fork at:
#   .github/workflows/docker-publish.yml
#
# Also add Dockerfile.catalog (from this module folder) to the root of your fork.

name: Build and Push Catalog API Image

on:
  push:
    branches: [main]
  workflow_dispatch:            # allows manual trigger from GitHub UI

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write           # needed to push to ghcr.io

    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        run: echo "OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.catalog
          push: true
          tags: |
            ghcr.io/${{ env.OWNER }}/catalog-api:latest
            ghcr.io/${{ env.OWNER }}/catalog-api:${{ github.sha }}
